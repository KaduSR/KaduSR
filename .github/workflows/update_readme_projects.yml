- name: Buscar repositórios do usuário
  uses: actions/github-script@v6
  with:
    github-token: ${{ secrets.GITHUB_TOKEN }}
    script: |
      const { data: repos } = await github.rest.repos.listForUser({
        username: "KaduSR",
        sort: "updated",
        direction: "desc",
        per_page: 5
      });

      const fs = require("fs");
      let content = "### 🚀 Projetos Recentes\n\n";
      for (const repo of repos) {
        content += `- [${repo.name}](${repo.html_url})\n`;
        content += `  > ${repo.description || "Sem descrição disponível"}\n\n`;
      }

      let readme = fs.readFileSync("README.md", "utf8");
      readme = readme.replace(
        /<!-- PROJECT-LIST:START -->([\\s\\S]*?)<!-- PROJECT-LIST:END -->/,
        `<!-- PROJECT-LIST:START -->\n${content}<!-- PROJECT-LIST:END -->`
      );
      fs.writeFileSync("README.md", readme);
